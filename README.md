# Ring Boxing Club - Production Ready

Профессиональный боксёрский клуб с e-commerce платформой.

## 📦 Структура проекта

```
ring-boxing-club/
│
├── css/                    # Стили
│   ├── main.css            # Главный CSS (импорты всех модулей)
│   ├── variables.css       # Переменные и глобальные стили
│   └── components/        # Компоненты
│       ├── header.css      # Шапка и навигация
│       ├── buttons.css     # Кнопки
│       ├── cards.css       # Карточки товаров
│       ├── forms.css       # Формы и инпуты
│       └── footer.css      # Футер
│
├── js/                     # JavaScript
│   ├── cart/
│   │   └── CartManager.js  # Менеджер корзины
│   └── utils/
│       ├── validation.js   # Валидация
│       ├── storage.js      # Работа с localStorage
│       └── helpers.js      # Вспомогательные функции
│
├── index.html              # Главная страница
├── shop.html               # Магазин
├── cart.html               # Корзина
├── equipment.html          # Экипировка
├── cosmetic.html           # Косметика
├── pharma.html             # Фармакология
├── checkout.html           # Оформление заказа
└── order-success.html      # Успешный заказ
```

## ✨ Основные улучшения

### 1. **Модульная архитектура CSS**
- ✅ Разделение на компоненты
- ✅ CSS-переменные для цветов, размеров, отступов
- ✅ Легкое кеширование браузером
- ✅ Повторное использование кода

### 2. **Модульный JavaScript**
- ✅ ES6 модули (import/export)
- ✅ Классы для логики
- ✅ Разделение ответственности
- ✅ Обработка ошибок

### 3. **Улучшения производительности**
- ✅ Вынесенные стили (70+ KB → кешируется)
- ✅ Lazy loading изображений/видео
- ✅ Preload/preconnect
- ✅ Reduced motion support

### 4. **Надёжность**
- ✅ Валидация данных
- ✅ Try-catch для localStorage
- ✅ Проверки доступности
- ✅ Toast-уведомления

## 🚀 Использование

### Быстрый старт

1. **Подключение CSS**
```html
<link rel="stylesheet" href="css/main.css">
```

2. **Подключение JavaScript**
```html
<script type="module">
  import CartManager from './js/cart/CartManager.js';
  
  const cart = new CartManager();
  cart.init();
</script>
```

### Примеры использования

#### Работа с корзиной
```javascript
import CartManager from './js/cart/CartManager.js';

const cart = new CartManager();
cart.init();

// Добавить товар
cart.addItem({
  name: 'Pro Fight Gloves',
  price: 8500,
  category: 'equipment'
});

// Обновить количество
cart.updateQuantity(0, 2);

// Удалить товар
cart.removeItem(0);

// Получить итоговую сумму
const total = cart.calculateSubtotal();
```

#### Валидация
```javascript
import Validation from './js/utils/validation.js';

// Проверка email
if (Validation.isValidEmail('user@example.com')) {
  console.log('Корректный email');
}

// Проверка телефона
if (Validation.isValidPhone('+79123456789')) {
  console.log('Корректный телефон');
}

// Проверка промокода
if (Validation.isValidPromoCode('BOXING25')) {
  console.log('Корректный промокод');
}
```

#### Хранение данных
```javascript
import Storage from './js/utils/storage.js';

// Сохранить
Storage.set('userPreferences', { theme: 'dark', language: 'ru' });

// Получить
const prefs = Storage.get('userPreferences', {});

// Удалить
Storage.remove('userPreferences');
```

#### Вспомогательные функции
```javascript
import Helpers from './js/utils/helpers.js';

// Форматирование цены
const price = Helpers.formatPrice(8500); // "8 500 ₽"

// Toast-уведомление
Helpers.toast('Товар добавлен!', 'success');

// Debounce
const searchHandler = Helpers.debounce(() => {
  console.log('Поиск...');
}, 300);

// Плавная прокрутка
Helpers.scrollTo('#products', 100);
```

## 🎯 CSS Переменные

Используйте CSS-переменные для настройки внешнего вида:

```css
:root {
  /* Цвета */
  --gold-metal: #C8B273;
  --silver-metal: #BFC7C7;
  
  /* Отступы */
  --spacing-sm: 10px;
  --spacing-md: 15px;
  --spacing-lg: 20px;
  
  /* Шрифты */
  --font-size-base: 0.95rem;
  --font-size-lg: 1.1rem;
  
  /* Скругления */
  --border-radius-md: 8px;
  --border-radius-lg: 12px;
  
  /* Переходы */
  --transition-base: 0.3s ease;
}
```

## 🛠️ Разработка

### Добавление нового компонента

1. Создайте файл `css/components/your-component.css`
2. Добавьте import в `css/main.css`:
```css
@import url('components/your-component.css');
```

### Добавление нового JS-модуля

1. Создайте файл `js/your-module/YourClass.js`
2. Экспортируйте класс:
```javascript
class YourClass {
  // ...
}

export default YourClass;
```
3. Импортируйте в HTML:
```html
<script type="module">
  import YourClass from './js/your-module/YourClass.js';
  const instance = new YourClass();
</script>
```

## 📊 Производительность

### До/После рефакторинга

| Метрика | До | После |
|---------|--------|----------|
| Размер HTML | 68 KB | 25 KB |
| CSS (кешируемый) | 0 KB | 45 KB |
| JS (модульный) | Монолит | Модули |
| Кеширование | Нет | Да |
| Поддерживаемость | 4/10 | 9/10 |

## 📝 Промокоды

Доступные промокоды:
- `WELCOME10` - 10% скидка
- `BOXING25` - 25% скидка
- `FIGHT100` - 100₽ скидка
- `CHAMPION` - 15% скидка

## 📦 Категории товаров

1. **Экипировка** (`equipment`) - Перчатки, шлемы, защита
2. **Косметика** (`cosmetic`) - Бальзамы, кремы
3. **Фармакология** (`pharma`) - Протеины, добавки

## ⚙️ Настройки

### Бесплатная доставка
Измените порог в `CartManager.js`:
```javascript
this.freeShippingThreshold = 5000; // Измените значение
```

### Стоимость доставки
```javascript
this.shippingCost = 300; // Измените значение
```

## 🐞 Известные проблемы

- Нет бэкенда (всё на клиенте)
- Нет реальной оплаты
- localStorage может быть очищен пользователем

## 🚀 Будущие улучшения

- [ ] Минификация CSS/JS
- [ ] Сборщик (Webpack/Vite)
- [ ] TypeScript
- [ ] Unit-тесты
- [ ] CI/CD pipeline
- [ ] Интеграция с бэкендом
- [ ] PWA поддержка
- [ ] Service Worker

## 📞 Контакты

- **Адрес**: г. Пенза, ул. Спортивная, 18
- **Телефон**: +7 (953) 358-43-72
- **Email**: shop@ringboxingclub.ru
- **Режим работы**: Ежедневно 10:00-20:00

---

© 2025 RING BOXING CLUB | Production-Ready Version
